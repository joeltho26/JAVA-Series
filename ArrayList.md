# Vectors:
- implements a dynamic array which means it can grow or shrink as required.
- it contains components that can be accessed using an integer index (get()).
- un-synchronized and not thread safe (means all threads will access the field members(variables/attributes) and methods in parallel)