# HashSet
- Elements are stored in random order based on their hashing values of the element
- data stored in them are in key-value pairs
- unlike hashset which has only the element and not key-value pairs
- they are not synchronized and not thread safe