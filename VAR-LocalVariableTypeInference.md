# VAR keyword:
- this is used in variable assignment when we automatically want java to determine its data types, object type, class type, etc.
- it is never used in method return type, declaration of class fields.
- it is never assigned to null literal
- it is always used with assignment of a value or output from an object or class.

  ```
   var thriller = new Adventure("Jungle Book");
  ```